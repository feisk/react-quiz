import React from 'react';
import classes from './style.module.scss';

const QuizCreator = props => {
    const { test } = props;

    return (
        <h1>Quiz Creator</h1>
    );
};

export { QuizCreator };